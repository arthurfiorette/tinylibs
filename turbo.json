{
  "$schema": "https://turborepo.org/schema.json",
  "baseBranch": "origin/main",
  "globalDependencies": ["./tsconfig.json"],
  "pipeline": {
    "build": {
      "outputs": ["**/dist"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["**/coverage"]
    }
  }
}
