{
  "$schema": "https://turborepo.org/schema.json",
  "baseBranch": "origin/main",
  "pipeline": {
    "build": {
      "outputs": ["**/dist"]
    },
    "changelog": {
      "outputs": ["**/CHANGELOG.md"],
      "cache": false
    },
    "test": {
      "outputs": ["**/coverage"]
    }
  }
}
